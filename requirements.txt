# Core dependencies
parakeet-mlx>=0.3.0
flask>=2.3.0
flask-cors>=4.0.0
flask-sock>=0.6.0
gradio>=4.19.0

# Audio processing
pydub>=0.25.0
sounddevice>=0.4.6
pyperclip>=1.8.0
scipy>=1.10.0

# Speaker diarization (optional - requires HuggingFace token)
# Note: pyannote.audio 4.x has breaking changes with torchcodec, use 3.x
pyannote.audio>=3.1.0,<4.0

# Data & visualization
pandas>=2.0.0
matplotlib>=3.7.0
numpy>=1.24.0,<2.4  # Numba requires NumPy <2.4

# HTTP client
requests>=2.31.0

# Local Model Server gateway (local_model_server.py) — OpenAI-compatible gateway/adapter
# proxying to a llama.cpp omni engine (text/image/audio) + /v1/audio/transcriptions
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
httpx>=0.27.0
soundfile>=0.12.0

# macOS menu bar app
rumps>=0.4.0
py2app>=0.28.0
