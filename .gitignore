# Virtual environments
/.venv
venv/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Build artifacts (py2app, setuptools, egg-info)
/build/
/dist/
*.egg-info/

# Daemon logs (launchd writes here when daemon runs under com.gui.parakeet)
/stderr.log
/stdout.log

# Local Model Server logs (engine + gateway launchd services)
/local_model_engine.log
/local_model_server.log

# OS / IDE noise
.DS_Store
.vscode/
.idea/

# Local agent worktrees (created by Claude Code background agents)
/.claude/worktrees/
